# 三国人物猜猜猜 - 快速启动指南

## 项目已完成！

我已经为你创建了一个完整的三国人物猜猜猜游戏，包含：

### 功能特点
- 100个三国历史人物数据库
- 支持单人练习或两人在线对战
- 详细的属性对比提示系统
- 人物关系系统
- 命令行和HTTP API两种游戏方式

### 快速启动

#### 方式1：命令行游戏（推荐）

```bash
cd game
python run.py
```

这会自动初始化数据库并启动命令行游戏界面。

#### 方式2：HTTP API服务器

```bash
cd game
python api.py
```

服务器会在 http://localhost:5000 启动，可以使用HTTP客户端调用API。

### 游戏玩法

1. **创建房间**：玩家1输入名字，系统生成房间码并直接开始游戏
2. **加入房间**（可选）：朋友可以输入房间码加入对战
3. **轮流猜测**：两人轮流输入三国人物名字（单人模式可连续猜测）
4. **查看提示**：系统返回属性对比结果
5. **猜对获胜**：猜中系统预设的人物即获胜

### 对比提示规则

猜测错误时，系统会返回：
- 姓：相同/不同
- 字：相同/不同/一方无字
- 生年：更早/更晚/相同/未知
- 卒年：更早/更晚/相同/未知
- 出生地：同省/不同省
- 势力：同势力/不同势力
- 身份：相同/不同
- 特质：列出相同特质
- 关系：如果存在关系会提示

### 文件说明

- `run.py` - 主启动脚本
- `cli.py` - 命令行客户端
- `api.py` - HTTP API接口
- `game.py` - 游戏核心逻辑
- `database.py` - 数据库操作
- `data.py` - 人物数据
- `init_data.py` - 数据初始化
- `demo.py` - 功能演示
- `test_game.py` - 功能测试

### 测试游戏

运行测试脚本验证功能：

```bash
cd game
python test_game.py
```

### 扩展人物数据

要添加新人物，编辑 `data.py` 文件中的 `CHARACTERS` 列表。

### 扩展关系数据

要添加新关系，编辑 `data.py` 文件中的 `RELATIONS` 列表。

## 开始游戏吧！

现在你可以运行 `python run.py` 开始游戏了！
